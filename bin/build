# hugo

# Enable google analytics
# --gc enable to run some cleanup tasks (remove unused cache files) after the build - DOESN'T delete old files though
# --cleanDestinationDir remove files from destination not found in static directories - DOES delete old files
env HUGO_ENV="production" hugo --gc --cleanDestinationDir
